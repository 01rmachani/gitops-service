name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    if: startsWith(github.event.pull_request.head.ref, 'feat/')

    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        run: echo "No test runner configured — placeholder pass"
